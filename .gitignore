node_modules/
dist/
*.local
.env
.env.*
.DS_Store

# Google service account / API credentials (JANGAN di-commit!)
gen-lang-client-*.json
*service-account*.json
*credentials*.json
secrets/

# runtime logs (dev servers)
*.log
dev.out
dev.err

# VPS / browser-extension tooling: commit scripts, but NEVER the chrome profiles
# (hundreds of MB of Login Data / Cookies — session credentials).
claude-extension-vps/chrome-profile-*/
